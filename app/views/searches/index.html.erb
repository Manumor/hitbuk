
<h1 id="univeristy_title"><%= @universities.name %> <img id="uni_picture" src=<%= @universities.image %>></a></h1>
		
<ul id="list">
	<% @courses.each do |course| %>
		<li ><%= link_to course.name, course_path(course) %></li>
	<% end %>
</ul>
